{
  "format": "KCML-SECRET-CHANNELS/1",
  "authority": "same fixed OWNER and server intent lineage; initiating channel never changes capabilities",
  "actions": [
    {
      "case": "SERVER_USE",
      "operation": "secret.resolve",
      "valueDestination": "bound server endpoint only; model receives handle and outcome",
      "audit": "secret.resolved",
      "guard": "exact useContext, target, operation, Secret version and current credential generation"
    },
    {
      "case": "REVEAL",
      "operation": "secret.value.read",
      "valueDestination": "OWNER-only Secret value panel; chat renders secure value card using same operation, not model prose",
      "audit": "secret.value.read",
      "guard": "explicit OWNER request to show value; current authenticated session"
    },
    {
      "case": "CHANGE",
      "operation": "secret.version.create",
      "valueDestination": "encrypted version storage",
      "audit": "secret.version.created",
      "guard": "schema valid; expected version CAS; secure form or chat input card"
    },
    {
      "case": "CREATE",
      "operation": "secret.create",
      "valueDestination": "encrypted Secret storage",
      "audit": "secret.created",
      "guard": "unique stable name, valid Secret kind and value"
    },
    {
      "case": "ROTATE",
      "operation": "secret.rotate",
      "valueDestination": "new encrypted version then guarded activation",
      "audit": "secret.rotated",
      "guard": "preview consumers, validate replacement, CAS active version, reconcile in-flight use"
    },
    {
      "case": "REMOVE",
      "operation": "secret.delete",
      "valueDestination": "tombstone and retention-controlled deletion",
      "audit": "secret.deleted",
      "guard": "impact and same confirmation as GUI; no silently broken active bindings"
    }
  ],
  "logging": "Audit IDs, version, destination, outcome and value fingerprint, never a value in general progress/error payloads; authorized evidence storage and explicit reveal remain supported",
  "clipboard": "OWNER gesture, Clipboard API only in secure context; failure renders selectable value panel; no false success",
  "modelBoundary": "Secret value not needed for server use; handle use preserves full functional parity; no new role or generic approval gate"
}
